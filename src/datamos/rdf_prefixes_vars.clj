(ns datamos.rdf-prefixes-vars
  "Contains the var atoms of datamos.rdf-prefixes. Exists to ease reloading of source files, as to ease development.")

(def prefixes (atom {}))

(def local-settings (atom {}))