(ns datamos.util-test
  (:require [clojure.test :refer :all]))

(deftest deep-merge
  )

(deftest replace-key
  )

(deftest select-into-submap
  )

(deftest select-submap-values
  )

(deftest apply-submap-values
  )

(deftest keyword->string
  )

(deftest string->keyword
  )

(deftest remove-nested-key
  )

(deftest component->queue-name
  )

(deftest select-subkeys
  )

(deftest select-subkey+
  )

(deftest select-subkeys+
  )

(deftest replace-sub-value
  )
